[package]
name = "nix-tidy"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.0", default-features = false, features = ["error-context", "help", "std", "usage"] }
rnix = { version = "0.14.0", default-features = false }
