diff --git a/Services/Hardware/BrightnessService.qml b/Services/Hardware/BrightnessService.qml
--- a/Services/Hardware/BrightnessService.qml
+++ b/Services/Hardware/BrightnessService.qml
@@ -307,7 +307,7 @@ Singleton {
 
           if (monitor.isAppleDisplay) {
             // Apple display format: single integer (0-101)
-            var val = parseInt(dataText);
+            var val = parseInt(dataText.replace("brightness ", ""));
             if (!isNaN(val)) {
               newBrightness = val / 101;
             }
@@ -402,7 +402,7 @@ Singleton {
 
           //Logger.i("Brightness", "Raw brightness data for", monitor.modelData.name + ":", dataText)
           if (monitor.isAppleDisplay) {
-            var val = parseInt(dataText);
+            var val = parseInt(dataText.replace("brightness ", ""));
             if (!isNaN(val)) {
               monitor.brightness = val / 101;
               Logger.d("Brightness", "Apple display brightness:", monitor.brightness);
