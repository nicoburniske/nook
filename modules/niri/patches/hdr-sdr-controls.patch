diff --git a/niri-config/src/lib.rs b/niri-config/src/lib.rs
index e11b9a95..b6920328 100644
--- a/niri-config/src/lib.rs
+++ b/niri-config/src/lib.rs
@@ -21,9 +21,9 @@ use std::io::Write as _;
 use std::path::{Path, PathBuf};
 use std::rc::Rc;
 
-use knuffel::errors::DecodeError;
 use knuffel::Decode as _;
-use miette::{miette, Context as _, IntoDiagnostic as _};
+use knuffel::errors::DecodeError;
+use miette::{Context as _, IntoDiagnostic as _, miette};
 
 #[macro_use]
 pub mod macros;
@@ -1177,6 +1177,8 @@ mod tests {
                                         203.0,
                                     ),
                                 ),
+                                sdr_brightness: None,
+                                sdr_saturation: None,
                             },
                         ),
                         mode: Some(
diff --git a/niri-config/src/output.rs b/niri-config/src/output.rs
index 70751047..153ee300 100644
--- a/niri-config/src/output.rs
+++ b/niri-config/src/output.rs
@@ -1,10 +1,10 @@
 use std::str::FromStr;
 
+use knuffel::Decode;
 use knuffel::ast::SpannedNode;
 use knuffel::decode::Context;
 use knuffel::errors::DecodeError;
 use knuffel::traits::ErrorSpan;
-use knuffel::Decode;
 use niri_ipc::{ConfiguredMode, HSyncPolarity, Transform, VSyncPolarity};
 
 use crate::gestures::HotCorners;
@@ -158,6 +158,12 @@ pub struct Hdr {
     /// in HDR mode. Defaults to 203 cd/m² (the BT.2408 reference white) when unset.
     #[knuffel(child, unwrap(argument))]
     pub reference_luminance: Option<FloatOrInt<0, 10000>>,
+    /// Multiplier applied to SDR content after mapping it into the HDR blend space.
+    #[knuffel(child, unwrap(argument))]
+    pub sdr_brightness: Option<FloatOrInt<0, 100>>,
+    /// Saturation multiplier applied to SDR content after mapping it into the HDR blend space.
+    #[knuffel(child, unwrap(argument))]
+    pub sdr_saturation: Option<FloatOrInt<0, 100>>,
 }
 
 /// When HDR engages on an `hdr`-enabled output.
diff --git a/niri-ipc/src/lib.rs b/niri-ipc/src/lib.rs
index 218e532c..238ae1a2 100644
--- a/niri-ipc/src/lib.rs
+++ b/niri-ipc/src/lib.rs
@@ -1103,6 +1103,43 @@ pub enum OutputAction {
         #[cfg_attr(feature = "clap", arg())]
         max_bpc: MaxBpc,
     },
+    /// Set the HDR mode.
+    HdrMode {
+        /// HDR mode to set.
+        #[cfg_attr(feature = "clap", arg())]
+        mode: HdrModeToSet,
+    },
+    /// Set the HDR SDR reference luminance, in nits.
+    HdrReferenceLuminance {
+        /// SDR white luminance to set.
+        #[cfg_attr(feature = "clap", arg())]
+        value: f64,
+    },
+    /// Set the HDR SDR brightness multiplier.
+    SdrBrightness {
+        /// SDR brightness multiplier to set.
+        #[cfg_attr(feature = "clap", arg())]
+        value: f64,
+    },
+    /// Set the HDR SDR saturation multiplier.
+    SdrSaturation {
+        /// SDR saturation multiplier to set.
+        #[cfg_attr(feature = "clap", arg())]
+        value: f64,
+    },
+}
+
+/// HDR mode to set.
+#[derive(Serialize, Deserialize, Debug, Clone, Copy, PartialEq, Eq)]
+#[cfg_attr(feature = "clap", derive(clap::ValueEnum))]
+#[cfg_attr(feature = "json-schema", derive(schemars::JsonSchema))]
+pub enum HdrModeToSet {
+    /// Disable HDR signalling on the output.
+    Off,
+    /// Enable HDR signalling while fullscreen HDR content is shown.
+    Auto,
+    /// Keep HDR signalling enabled.
+    On,
 }
 
 /// Output mode to set.
@@ -1236,6 +1273,16 @@ pub struct Output {
     pub logical: Option<LogicalOutput>,
     /// Maximum bits per channel (bit depth), if known.
     pub max_bpc: Option<u8>,
+    /// Whether the output supports HDR signalling.
+    pub hdr_supported: bool,
+    /// Effective HDR mode.
+    pub hdr_mode: HdrModeToSet,
+    /// Effective HDR SDR reference luminance, in nits.
+    pub hdr_reference_luminance: f64,
+    /// Effective SDR brightness multiplier in HDR mode.
+    pub sdr_brightness: f64,
+    /// Effective SDR saturation multiplier in HDR mode.
+    pub sdr_saturation: f64,
 }
 
 /// Output mode.
@@ -2094,6 +2141,20 @@ impl OutputAction {
                 }
                 Ok(())
             }
+            OutputAction::HdrReferenceLuminance { value } => {
+                ensure!(
+                    value.is_finite() && (0.0..=10000.0).contains(value),
+                    "hdr reference luminance {value} must be between 0 and 10000"
+                );
+                Ok(())
+            }
+            OutputAction::SdrBrightness { value } | OutputAction::SdrSaturation { value } => {
+                ensure!(
+                    value.is_finite() && (0.0..=100.0).contains(value),
+                    "sdr value {value} must be between 0 and 100"
+                );
+                Ok(())
+            }
             _ => Ok(()),
         }
     }
diff --git a/src/backend/headless.rs b/src/backend/headless.rs
index 4e872d45..7dbca6b9 100644
--- a/src/backend/headless.rs
+++ b/src/backend/headless.rs
@@ -111,6 +111,11 @@ impl Headless {
                 vrr_enabled: false,
                 logical: Some(logical_output(&output)),
                 max_bpc: None,
+                hdr_supported: false,
+                hdr_mode: niri_ipc::HdrModeToSet::Off,
+                hdr_reference_luminance: crate::render_helpers::blend::DEFAULT_REFERENCE_LUMINANCE,
+                sdr_brightness: 1.,
+                sdr_saturation: 1.,
             },
         );
 
diff --git a/src/backend/tty.rs b/src/backend/tty.rs
index 6341adc8..04e333bb 100644
--- a/src/backend/tty.rs
+++ b/src/backend/tty.rs
@@ -11,17 +11,17 @@ use std::sync::{Arc, Mutex};
 use std::time::Duration;
 use std::{io, mem};
 
-use anyhow::{anyhow, bail, ensure, Context};
+use anyhow::{Context, anyhow, bail, ensure};
 use bytemuck::cast_slice_mut;
 use drm_ffi::drm_mode_modeinfo;
 use libc::dev_t;
 use niri_config::output::{HdrMode, Modeline};
 use niri_config::{Config, OutputName};
 use niri_ipc::{HSyncPolarity, VSyncPolarity};
+use smithay::backend::allocator::Fourcc;
 use smithay::backend::allocator::dmabuf::Dmabuf;
 use smithay::backend::allocator::format::FormatSet;
 use smithay::backend::allocator::gbm::{GbmAllocator, GbmBufferFlags, GbmDevice};
-use smithay::backend::allocator::Fourcc;
 use smithay::backend::drm::compositor::{DrmCompositor, FrameFlags, PrimaryPlaneElement};
 use smithay::backend::drm::exporter::gbm::GbmFramebufferExporter;
 use smithay::backend::drm::{
@@ -46,8 +46,8 @@ use smithay::reexports::calloop::{Dispatcher, LoopHandle, RegistrationToken};
 use smithay::reexports::drm::control::atomic::AtomicModeReq;
 use smithay::reexports::drm::control::dumbbuffer::DumbBuffer;
 use smithay::reexports::drm::control::{
-    self, connector, crtc, plane, property, AtomicCommitFlags, Device, Mode as DrmMode, ModeFlags,
-    ModeTypeFlags, PlaneType, ResourceHandle,
+    self, AtomicCommitFlags, Device, Mode as DrmMode, ModeFlags, ModeTypeFlags, PlaneType,
+    ResourceHandle, connector, crtc, plane, property,
 };
 use smithay::reexports::gbm::Modifier;
 use smithay::reexports::input::Libinput;
@@ -72,7 +72,9 @@ use smithay::wayland::color::management::{
     PrimariesOption as CmPrimariesOption, TransferFunction as CmTransferFunction,
 };
 
-use crate::render_helpers::blend::{self, set_frame_blend, DEFAULT_REFERENCE_LUMINANCE};
+use crate::render_helpers::blend::{
+    self, BlendSpace, DEFAULT_REFERENCE_LUMINANCE, set_frame_blend,
+};
 
 use crate::niri::{Niri, RedrawState, State};
 use crate::render_helpers::debug::draw_damage;
@@ -90,8 +92,8 @@ const SDR_COLOR_FORMATS: [Fourcc; 4] = [
     Fourcc::Abgr8888,
 ];
 
-/// Scanout formats offered on HDR outputs: 10-bit preferred (needed so the PQ signal isn't crushed),
-/// falling back to 8-bit if the GPU won't scan out 10-bit at the chosen mode.
+/// Scanout formats offered on HDR outputs: 10-bit preferred (needed so the PQ signal isn't
+/// crushed), falling back to 8-bit if the GPU won't scan out 10-bit at the chosen mode.
 const HDR_COLOR_FORMATS: [Fourcc; 8] = [
     Fourcc::Xrgb2101010,
     Fourcc::Xbgr2101010,
@@ -421,7 +423,7 @@ struct Surface {
     /// The blend space of the last rendered frame: `Some(reference luminance)` = HDR, `None`
     /// = SDR. Blend changes alter shader output without damaging anything, so a change forces
     /// a full redraw.
-    last_blend: Option<Option<f64>>,
+    last_blend: Option<Option<BlendSpace>>,
     dmabuf_feedback: Option<SurfaceDmabufFeedback>,
     gamma_props: Option<GammaProps>,
     /// Gamma change to apply upon session resume.
@@ -2209,7 +2211,7 @@ impl Tty {
         // The connector state is only *staged* here; smithay applies it inside its own commit
         // as a single atomic modeset together with mode, CRTC and plane state (committing
         // connector color properties standalone hangs some drivers, notably nvidia).
-        let (blend_hdr, hdr_content, reference_luminance) = {
+        let (blend_hdr, hdr_content, blend_space) = {
             let config = self.config.borrow();
             let output_config = config.outputs.find(&surface.name);
             let hdr_config = output_config.and_then(|o| o.hdr.clone());
@@ -2223,6 +2225,21 @@ impl Tty {
                 .and_then(|h| h.reference_luminance)
                 .map(|v| v.0)
                 .unwrap_or(DEFAULT_REFERENCE_LUMINANCE);
+            let sdr_brightness = hdr_config
+                .as_ref()
+                .and_then(|h| h.sdr_brightness)
+                .map(|v| v.0)
+                .unwrap_or(1.);
+            let sdr_saturation = hdr_config
+                .as_ref()
+                .and_then(|h| h.sdr_saturation)
+                .map(|v| v.0)
+                .unwrap_or(1.);
+            let blend_space = BlendSpace {
+                reference_luminance,
+                sdr_brightness,
+                sdr_saturation,
+            };
             drop(config);
 
             let hdr_desc = hdr_allowed
@@ -2278,12 +2295,12 @@ impl Tty {
                 }
             }
 
-            (blend_hdr, hdr_desc.is_some(), reference_luminance)
+            (blend_hdr, hdr_desc.is_some(), blend_space)
         };
 
         // A blend-space change alters what every shader outputs without any element damage;
         // force a full redraw.
-        let blend = blend_hdr.then_some(reference_luminance);
+        let blend = blend_hdr.then_some(blend_space);
         if surface.last_blend != Some(blend) {
             surface.last_blend = Some(blend);
             surface.compositor.reset_buffers();
@@ -2639,6 +2656,28 @@ impl Tty {
                         .map(|v| v as u8)
                 });
 
+                let config = self.config.borrow();
+                let output_config = config.outputs.find(&output_name);
+                let hdr_config = output_config.and_then(|config| config.hdr.as_ref());
+                let hdr_mode = match hdr_config.map(|hdr| hdr.mode) {
+                    Some(HdrMode::Auto) => niri_ipc::HdrModeToSet::Auto,
+                    Some(HdrMode::On) => niri_ipc::HdrModeToSet::On,
+                    None => niri_ipc::HdrModeToSet::Off,
+                };
+                let hdr_reference_luminance = hdr_config
+                    .and_then(|hdr| hdr.reference_luminance)
+                    .map(|value| value.0)
+                    .unwrap_or(DEFAULT_REFERENCE_LUMINANCE);
+                let sdr_brightness = hdr_config
+                    .and_then(|hdr| hdr.sdr_brightness)
+                    .map(|value| value.0)
+                    .unwrap_or(1.);
+                let sdr_saturation = hdr_config
+                    .and_then(|hdr| hdr.sdr_saturation)
+                    .map(|value| value.0)
+                    .unwrap_or(1.);
+                drop(config);
+
                 let ipc_output = niri_ipc::Output {
                     name: connector_name,
                     make: output_name.make.unwrap_or_else(|| "Unknown".into()),
@@ -2652,6 +2691,11 @@ impl Tty {
                     vrr_enabled,
                     logical,
                     max_bpc,
+                    hdr_supported: surface.is_some_and(|surface| surface.hdr_supported),
+                    hdr_mode,
+                    hdr_reference_luminance,
+                    sdr_brightness,
+                    sdr_saturation,
                 };
 
                 ipc_outputs.insert(id, ipc_output);
@@ -3881,12 +3925,12 @@ fn make_output_name(
 unsafe fn init_libinput_plugin_system(libinput: &Libinput) {
     #[cfg(have_libinput_plugin_system)]
     unsafe {
-        use std::ffi::{c_char, c_int, CString};
+        use std::ffi::{CString, c_char, c_int};
         use std::os::unix::ffi::OsStringExt;
 
         use directories::BaseDirs;
-        use input::ffi::libinput;
         use input::AsRaw as _;
+        use input::ffi::libinput;
 
         extern "C" {
             fn libinput_plugin_system_append_path(libinput: *const libinput, path: *const c_char);
@@ -3921,11 +3965,10 @@ mod tests {
     use insta::assert_debug_snapshot;
     use niri_config::output::Modeline;
     use niri_ipc::{HSyncPolarity, VSyncPolarity};
-
     use smithay::wayland::color::management::ImageDescription;
 
     use crate::backend::tty::{
-        build_hdr_metadata, calculate_drm_mode_from_modeline, calculate_mode_cvt, EdidHdrInfo,
+        EdidHdrInfo, build_hdr_metadata, calculate_drm_mode_from_modeline, calculate_mode_cvt,
     };
 
     #[test]
diff --git a/src/backend/winit.rs b/src/backend/winit.rs
index 8ac4d777..3a5ac9e1 100644
--- a/src/backend/winit.rs
+++ b/src/backend/winit.rs
@@ -98,6 +98,11 @@ impl Winit {
                 vrr_enabled: false,
                 logical: Some(logical_output(&output)),
                 max_bpc: None,
+                hdr_supported: false,
+                hdr_mode: niri_ipc::HdrModeToSet::Off,
+                hdr_reference_luminance: crate::render_helpers::blend::DEFAULT_REFERENCE_LUMINANCE,
+                sdr_brightness: 1.,
+                sdr_saturation: 1.,
             },
         )])));
 
diff --git a/src/ipc/client.rs b/src/ipc/client.rs
index 1ec51c44..131ea54b 100644
--- a/src/ipc/client.rs
+++ b/src/ipc/client.rs
@@ -569,6 +569,11 @@ fn print_output(output: Output) -> anyhow::Result<()> {
         vrr_enabled,
         logical,
         max_bpc,
+        hdr_supported: _,
+        hdr_mode: _,
+        hdr_reference_luminance: _,
+        sdr_brightness: _,
+        sdr_saturation: _,
     } = output;
 
     let serial = serial.as_deref().unwrap_or("Unknown");
diff --git a/src/niri.rs b/src/niri.rs
index 6c558b26..e8b184bb 100644
--- a/src/niri.rs
+++ b/src/niri.rs
@@ -14,7 +14,7 @@ use _server_decoration::server::org_kde_kwin_server_decoration_manager::Mode as
 use anyhow::{bail, ensure, Context};
 use calloop::futures::Scheduler;
 use niri_config::debug::PreviewRender;
-use niri_config::output::{HdrMode, MaxBpc};
+use niri_config::output::{Hdr, HdrMode, MaxBpc};
 use niri_config::{
     Config, FloatOrInt, Key, Modifiers, OutputName, TrackLayout, WarpMouseToFocusMode,
     WorkspaceReference, Xkb,
@@ -1948,6 +1948,27 @@ impl State {
                 }
             }
             niri_ipc::OutputAction::MaxBpc { max_bpc } => config.max_bpc = Some(MaxBpc(max_bpc)),
+            niri_ipc::OutputAction::HdrMode { mode } => match mode {
+                niri_ipc::HdrModeToSet::Off => config.hdr = None,
+                niri_ipc::HdrModeToSet::Auto => {
+                    config.hdr.get_or_insert_with(Hdr::default).mode = HdrMode::Auto;
+                }
+                niri_ipc::HdrModeToSet::On => {
+                    config.hdr.get_or_insert_with(Hdr::default).mode = HdrMode::On;
+                }
+            },
+            niri_ipc::OutputAction::HdrReferenceLuminance { value } => {
+                config
+                    .hdr
+                    .get_or_insert_with(Hdr::default)
+                    .reference_luminance = Some(FloatOrInt(value));
+            }
+            niri_ipc::OutputAction::SdrBrightness { value } => {
+                config.hdr.get_or_insert_with(Hdr::default).sdr_brightness = Some(FloatOrInt(value));
+            }
+            niri_ipc::OutputAction::SdrSaturation { value } => {
+                config.hdr.get_or_insert_with(Hdr::default).sdr_saturation = Some(FloatOrInt(value));
+            }
         });
 
         self.reload_output_config();
diff --git a/src/render_helpers/blend.rs b/src/render_helpers/blend.rs
index 9b9f9dd4..60a55dfa 100644
--- a/src/render_helpers/blend.rs
+++ b/src/render_helpers/blend.rs
@@ -30,6 +30,13 @@ use crate::backend::tty::{TtyFrame, TtyRenderer, TtyRendererError};
 /// Default SDR reference white in cd/m² (BT.2408).
 pub const DEFAULT_REFERENCE_LUMINANCE: f64 = 203.;
 
+#[derive(Debug, Clone, Copy, PartialEq)]
+pub struct BlendSpace {
+    pub reference_luminance: f64,
+    pub sdr_brightness: f64,
+    pub sdr_saturation: f64,
+}
+
 /// How a surface's content relates to the output blend space, derived from its committed
 /// image description.
 #[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
@@ -90,6 +97,8 @@ impl ContentColor {
 pub struct FrameBlendState {
     hdr_pq: Cell<bool>,
     ref_lum_scale: Cell<f32>,
+    sdr_brightness: Cell<f32>,
+    sdr_saturation: Cell<f32>,
 }
 
 impl FrameBlendState {
@@ -108,12 +117,16 @@ impl FrameBlendState {
 
     /// Marks the frames rendered from now on as HDR with the given SDR reference luminance,
     /// or as SDR (`None`).
-    pub fn set(renderer: &mut GlesRenderer, reference_luminance: Option<f64>) {
+    pub fn set(renderer: &mut GlesRenderer, blend: Option<BlendSpace>) {
         let state = Self::get(renderer);
-        match reference_luminance {
-            Some(lum) => {
+        match blend {
+            Some(blend) => {
                 state.hdr_pq.set(true);
-                state.ref_lum_scale.set((lum / 10000.) as f32);
+                state
+                    .ref_lum_scale
+                    .set((blend.reference_luminance / 10000.) as f32);
+                state.sdr_brightness.set(blend.sdr_brightness as f32);
+                state.sdr_saturation.set(blend.sdr_saturation as f32);
             }
             None => state.hdr_pq.set(false),
         }
@@ -127,13 +140,18 @@ impl FrameBlendState {
             .set((reference_luminance / 10000.) as f32);
     }
 
-    fn values_from_frame(frame: &GlesFrame) -> (bool, f32) {
+    fn values_from_frame(frame: &GlesFrame) -> (bool, f32, f32, f32) {
         let state: &Self = frame
             .egl_context()
             .user_data()
             .get()
             .expect("FrameBlendState::init() must be called when creating the renderer");
-        (state.hdr_pq.get(), state.ref_lum_scale.get())
+        (
+            state.hdr_pq.get(),
+            state.ref_lum_scale.get(),
+            state.sdr_brightness.get(),
+            state.sdr_saturation.get(),
+        )
     }
 
     pub fn is_hdr_frame(frame: &GlesFrame) -> bool {
@@ -145,8 +163,8 @@ impl FrameBlendState {
     }
 
     /// The `niri_blend` uniform values for content already rendered in the frame blend space.
-    pub fn uniforms_for_blend_space(frame: &GlesFrame) -> [Uniform<'static>; 6] {
-        let (_, scale) = Self::values_from_frame(frame);
+    pub fn uniforms_for_blend_space(frame: &GlesFrame) -> [Uniform<'static>; 8] {
+        let (_, scale, brightness, saturation) = Self::values_from_frame(frame);
         [
             Uniform::new("niri_hdr_pq", 0.0f32),
             Uniform::new("niri_ref_lum_scale", scale),
@@ -154,11 +172,13 @@ impl FrameBlendState {
             Uniform::new("niri_linear_scale", 0.0f32),
             Uniform::new("niri_linear_to_ref", 0.0f32),
             Uniform::new("niri_hdr_to_sdr", 0.0f32),
+            Uniform::new("niri_sdr_brightness", brightness),
+            Uniform::new("niri_sdr_saturation", saturation),
         ]
     }
 
     /// The `niri_blend` uniform values for a draw of SDR content in this frame.
-    pub fn uniforms(frame: &GlesFrame) -> [Uniform<'static>; 6] {
+    pub fn uniforms(frame: &GlesFrame) -> [Uniform<'static>; 8] {
         Self::uniforms_for_content(frame, ContentColor::Sdr)
     }
 
@@ -166,8 +186,8 @@ impl FrameBlendState {
     /// exempts client content already encoded in HDR PQ from SDR-to-HDR conversion,
     /// [`ContentColor::Linear`] selects the absolute extended-linear encode. HDR PQ content is
     /// converted back to SDR when drawn into SDR capture buffers.
-    pub fn uniforms_for_content(frame: &GlesFrame, content: ContentColor) -> [Uniform<'static>; 6] {
-        let (hdr_pq, scale) = Self::values_from_frame(frame);
+    pub fn uniforms_for_content(frame: &GlesFrame, content: ContentColor) -> [Uniform<'static>; 8] {
+        let (hdr_pq, scale, brightness, saturation) = Self::values_from_frame(frame);
         let sdr_to_hdr = hdr_pq && content != ContentColor::HdrPq;
         let hdr_to_sdr = !hdr_pq && content == ContentColor::HdrPq;
         let (linear, linear_scale, linear_to_ref) = match content {
@@ -189,6 +209,8 @@ impl FrameBlendState {
             Uniform::new("niri_linear_scale", linear_scale),
             Uniform::new("niri_linear_to_ref", linear_to_ref),
             Uniform::new("niri_hdr_to_sdr", if hdr_to_sdr { 1.0f32 } else { 0.0 }),
+            Uniform::new("niri_sdr_brightness", brightness),
+            Uniform::new("niri_sdr_saturation", saturation),
         ]
     }
 }
@@ -200,12 +222,14 @@ impl FrameBlendState {
 /// solid colors are encoded on the CPU, and niri's own shader programs read the frame blend
 /// state for their `niri_blend` stage. Call with `None` after rendering the output so
 /// screencasts, screenshots and other outputs stay SDR.
-pub fn set_frame_blend(renderer: &mut GlesRenderer, reference_luminance: Option<f64>) {
-    FrameBlendState::set(renderer, reference_luminance);
-
-    match reference_luminance {
-        Some(lum) => {
-            let scale = (lum / 10000.) as f32;
+pub fn set_frame_blend(renderer: &mut GlesRenderer, blend: Option<BlendSpace>) {
+    FrameBlendState::set(renderer, blend);
+
+    match blend {
+        Some(blend) => {
+            let scale = (blend.reference_luminance / 10000.) as f32;
+            let brightness = blend.sdr_brightness as f32;
+            let saturation = blend.sdr_saturation as f32;
             let program = Shaders::get(renderer).texture_hdr.clone();
             if let Some(program) = program {
                 renderer.set_default_tex_program_override(Some((
@@ -220,13 +244,16 @@ pub fn set_frame_blend(renderer: &mut GlesRenderer, reference_luminance: Option<
                         Uniform::new("niri_linear_scale", 0.0f32),
                         Uniform::new("niri_linear_to_ref", 0.0f32),
                         Uniform::new("niri_hdr_to_sdr", 0.0f32),
+                        Uniform::new("niri_sdr_brightness", brightness),
+                        Uniform::new("niri_sdr_saturation", saturation),
                     ],
                 )));
             } else {
                 warn!("HDR texture shader missing; SDR content will render raw");
             }
-            renderer
-                .set_solid_color_transform(Some(Box::new(move |color| srgb_to_pq(color, scale))));
+            renderer.set_solid_color_transform(Some(Box::new(move |color| {
+                srgb_to_pq(color, scale, brightness, saturation)
+            })));
         }
         None => {
             renderer.set_default_tex_program_override(None);
@@ -245,7 +272,12 @@ pub fn set_sdr_capture_blend(renderer: &mut GlesRenderer, reference_luminance: f
 
 /// CPU counterpart of the shaders' `niri_blend`: encodes an electrical sRGB premultiplied
 /// color into PQ/BT.2020 for the given SDR reference luminance scale (reference / 10000).
-pub fn srgb_to_pq(color: Color32F, ref_lum_scale: f32) -> Color32F {
+pub fn srgb_to_pq(
+    color: Color32F,
+    ref_lum_scale: f32,
+    sdr_brightness: f32,
+    sdr_saturation: f32,
+) -> Color32F {
     let a = color.a();
     let unpremul = |c: f32| if a > 0. { c / a } else { c };
 
@@ -268,10 +300,21 @@ pub fn srgb_to_pq(color: Color32F, ref_lum_scale: f32) -> Color32F {
     let g2020 = 0.069097 * r + 0.919540 * g + 0.011362 * b;
     let b2020 = 0.016391 * r + 0.088013 * g + 0.895595 * b;
 
+    let mut r = pq(r2020 * ref_lum_scale);
+    let mut g = pq(g2020 * ref_lum_scale);
+    let mut b = pq(b2020 * ref_lum_scale);
+
+    if sdr_saturation != 1. {
+        let y = 0.2627 * r + 0.6780 * g + 0.0593 * b;
+        r = y + (r - y) * sdr_saturation;
+        g = y + (g - y) * sdr_saturation;
+        b = y + (b - y) * sdr_saturation;
+    }
+
     Color32F::new(
-        pq(r2020 * ref_lum_scale) * a,
-        pq(g2020 * ref_lum_scale) * a,
-        pq(b2020 * ref_lum_scale) * a,
+        r * sdr_brightness * a,
+        g * sdr_brightness * a,
+        b * sdr_brightness * a,
         a,
     )
 }
@@ -557,20 +600,23 @@ mod tests {
         let scale = (203. / 10000.) as f32;
 
         // Opaque white at reference luminance 203 cd/m²: PQ(0.0203) ≈ 0.5806.
-        let white = srgb_to_pq(Color32F::new(1., 1., 1., 1.), scale);
+        let white = srgb_to_pq(Color32F::new(1., 1., 1., 1.), scale, 1., 1.);
         assert!((white.r() - 0.5806).abs() < 0.002, "got {}", white.r());
         // BT.709 white maps to BT.2020 white (rows sum to 1) => neutral stays neutral.
         assert!((white.r() - white.g()).abs() < 0.0005);
         assert!((white.g() - white.b()).abs() < 0.0005);
 
         // Black stays (essentially) black — PQ(0) is ~4e-7 — and alpha is preserved.
-        let black = srgb_to_pq(Color32F::new(0., 0., 0., 0.5), scale);
+        let black = srgb_to_pq(Color32F::new(0., 0., 0., 0.5), scale, 1., 1.);
         assert!(black.r() < 1e-6, "got {}", black.r());
         assert_eq!(black.a(), 0.5);
 
         // Premultiplied 50% white: unpremultiplied value is 1.0, so the encoded result is
         // the white point rescaled by alpha.
-        let half = srgb_to_pq(Color32F::new(0.5, 0.5, 0.5, 0.5), scale);
+        let half = srgb_to_pq(Color32F::new(0.5, 0.5, 0.5, 0.5), scale, 1., 1.);
         assert!((half.r() - white.r() * 0.5).abs() < 0.0005);
+
+        let brighter = srgb_to_pq(Color32F::new(1., 1., 1., 1.), scale, 1.2, 1.);
+        assert!((brighter.r() - white.r() * 1.2).abs() < 0.0005);
     }
 }
diff --git a/src/render_helpers/shader_element.rs b/src/render_helpers/shader_element.rs
index 9e59a020..e89b52b9 100644
--- a/src/render_helpers/shader_element.rs
+++ b/src/render_helpers/shader_element.rs
@@ -3,13 +3,13 @@ use std::ffi::CString;
 use std::rc::Rc;
 
 use glam::{Mat3, Vec2};
+use smithay::backend::renderer::DebugFlags;
 use smithay::backend::renderer::element::{Element, Id, Kind, RenderElement, UnderlyingStorage};
 use smithay::backend::renderer::gles::{
-    ffi, link_program, Capability, GlesError, GlesFrame, GlesRenderer, GlesTexture, Uniform,
-    UniformDesc, UniformName, UniformType,
+    Capability, GlesError, GlesFrame, GlesRenderer, GlesTexture, Uniform, UniformDesc, UniformName,
+    UniformType, ffi, link_program,
 };
 use smithay::backend::renderer::utils::{CommitCounter, OpaqueRegions};
-use smithay::backend::renderer::DebugFlags;
 use smithay::utils::user_data::UserDataMap;
 use smithay::utils::{Buffer, Logical, Physical, Point, Rectangle, Scale, Size};
 
@@ -177,6 +177,8 @@ impl ShaderProgram {
         additional_uniforms.push(UniformName::new("niri_linear_scale", UniformType::_1f));
         additional_uniforms.push(UniformName::new("niri_linear_to_ref", UniformType::_1f));
         additional_uniforms.push(UniformName::new("niri_hdr_to_sdr", UniformType::_1f));
+        additional_uniforms.push(UniformName::new("niri_sdr_brightness", UniformType::_1f));
+        additional_uniforms.push(UniformName::new("niri_sdr_saturation", UniformType::_1f));
 
         renderer.with_context(move |gl| unsafe {
             compile_program(gl, &src, &additional_uniforms, texture_uniforms)
diff --git a/src/render_helpers/shaders/hdr.frag b/src/render_helpers/shaders/hdr.frag
index 80b7d3dc..a7a41dd4 100644
--- a/src/render_helpers/shaders/hdr.frag
+++ b/src/render_helpers/shaders/hdr.frag
@@ -15,6 +15,8 @@ uniform float niri_linear;
 uniform float niri_linear_scale;
 uniform float niri_linear_to_ref;
 uniform float niri_hdr_to_sdr;
+uniform float niri_sdr_brightness;
+uniform float niri_sdr_saturation;
 
 vec3 niri_pq_inv_eotf(vec3 lin) {
     const float pq_m1 = 0.1593017578125;
@@ -107,5 +109,12 @@ vec4 niri_blend(vec4 color) {
     rgb = to_bt2020 * rgb;
 
     rgb = niri_pq_inv_eotf(rgb * niri_ref_lum_scale);
+
+    if (niri_sdr_saturation != 1.0) {
+        float y = dot(rgb, vec3(0.2627, 0.6780, 0.0593));
+        rgb = mix(vec3(y), rgb, niri_sdr_saturation);
+    }
+    rgb *= niri_sdr_brightness;
+
     return vec4(rgb * a, a);
 }
diff --git a/src/render_helpers/shaders/mod.rs b/src/render_helpers/shaders/mod.rs
index f6625c9e..608736b9 100644
--- a/src/render_helpers/shaders/mod.rs
+++ b/src/render_helpers/shaders/mod.rs
@@ -48,6 +48,8 @@ impl Shaders {
                     UniformName::new("niri_linear_scale", UniformType::_1f),
                     UniformName::new("niri_linear_to_ref", UniformType::_1f),
                     UniformName::new("niri_hdr_to_sdr", UniformType::_1f),
+                    UniformName::new("niri_sdr_brightness", UniformType::_1f),
+                    UniformName::new("niri_sdr_saturation", UniformType::_1f),
                 ],
             )
             .map_err(|err| {
@@ -133,6 +135,8 @@ impl Shaders {
                     UniformName::new("niri_linear_scale", UniformType::_1f),
                     UniformName::new("niri_linear_to_ref", UniformType::_1f),
                     UniformName::new("niri_hdr_to_sdr", UniformType::_1f),
+                    UniformName::new("niri_sdr_brightness", UniformType::_1f),
+                    UniformName::new("niri_sdr_saturation", UniformType::_1f),
                 ],
             )
             .map_err(|err| {
@@ -162,6 +166,8 @@ impl Shaders {
                     UniformName::new("niri_linear_scale", UniformType::_1f),
                     UniformName::new("niri_linear_to_ref", UniformType::_1f),
                     UniformName::new("niri_hdr_to_sdr", UniformType::_1f),
+                    UniformName::new("niri_sdr_brightness", UniformType::_1f),
+                    UniformName::new("niri_sdr_saturation", UniformType::_1f),
                 ],
             )
             .map_err(|err| {
@@ -186,6 +192,8 @@ impl Shaders {
                     UniformName::new("niri_linear_scale", UniformType::_1f),
                     UniformName::new("niri_linear_to_ref", UniformType::_1f),
                     UniformName::new("niri_hdr_to_sdr", UniformType::_1f),
+                    UniformName::new("niri_sdr_brightness", UniformType::_1f),
+                    UniformName::new("niri_sdr_saturation", UniformType::_1f),
                 ],
             )
             .map_err(|err| {
